name: test_env
channels:
    - defaults
    - http://conda.anaconda.org/pvlib
dependencies:
    - python=3.4
    - numpy
    - scipy
    - pandas
    - nose
    - pvlib
    - matplotlib
    - ephem
    - numba
    - pip:
        - coveralls